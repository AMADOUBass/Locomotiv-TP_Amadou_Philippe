<UserControl x:Class="Locomotiv.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Locomotiv.View"
             xmlns:converters="clr-namespace:Locomotiv.Utils.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="{Binding WelcomeMessage, UpdateSourceTrigger=PropertyChanged}"
                       FontSize="24"
                       FontWeight="Bold"
                       Margin="10"/>

            <!-- Admin dashboard control -->
            <local:AdminDashboardView Visibility="{Binding IsAdmin, Converter={StaticResource BoolToVisibilityConverter}}"/>

            <!-- Employe dashboard control -->
            <local:EmployeDashboardView Visibility="{Binding IsEmploye, Converter={StaticResource BoolToVisibilityConverter}}"/>
        </StackPanel>
    </Grid>
</UserControl>