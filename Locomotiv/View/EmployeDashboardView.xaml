<UserControl x:Class="Locomotiv.View.EmployeDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Locomotiv.View"
             xmlns:converters="clr-namespace:Locomotiv.Utils.Converters"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <Grid Margin="2">
        <StackPanel Margin="20">
            <StackPanel Margin="20">
                <TextBlock Text="Station assignée :" FontWeight="Bold" FontSize="16"/>
                <TextBlock Text="{Binding StationAssignee.Nom , TargetNullValue='Aucune station assignée'}"  Foreground="Black"/>
                <TextBlock Text="{Binding StationAssignee.Localisation}" Foreground="Black"/>
                <TextBlock Text="Capacité max :"/>
                <TextBlock Text="{Binding StationAssignee.CapaciteMaxTrains}" Foreground="Black"/>
                <TextBlock Text="{Binding GetType.Name}" Foreground="Red"/>
            </StackPanel>
        </StackPanel>

    </Grid>
</UserControl>